/**
 * config.gs — 設定値の一元管理
 *
 * カスタマイズする際はこのファイルだけ変更すれば OK。
 * 顧客ごとに会社名・税率・シート名を変えるだけで使い回せる。
 */

// ───────────────────────────────────────
// 自社情報（見積書・請求書のヘッダーに表示）
// ───────────────────────────────────────
const COMPANY = {
  name: '株式会社サンプル金属加工',          // 自社名
  zip: '〒530-0001',                        // 郵便番号
  address: '大阪府大阪市北区梅田1-1-1',      // 住所
  tel: '06-1234-5678',                      // 電話番号
  fax: '06-1234-5679',                      // FAX
  bank: '三菱UFJ銀行 梅田支店 普通 1234567', // 振込先
  representative: '山田太郎',                // 代表者名
};

// ───────────────────────────────────────
// シート名（スプレッドシートのタブ名と一致させる）
// ───────────────────────────────────────
const SHEET_NAMES = {
  products: '商品マスタ',       // 商品一覧
  clients: '取引先マスタ',      // 取引先一覧
  deals: '案件データ',          // メインの案件管理
  estimate: '見積書',           // 自動生成される見積書
  delivery: '納品書',           // 自動生成される納品書
  invoice: '請求書',            // 自動生成される請求書
  report: '月次売上レポート',    // 月次集計
};

// ───────────────────────────────────────
// 案件データシートの列番号（1始まり）
// ───────────────────────────────────────
const COL = {
  dealId: 1,         // A: 案件番号
  date: 2,           // B: 案件日付
  clientName: 3,     // C: 取引先名
  status: 4,         // D: ステータス
  productName: 5,    // E: 商品名
  spec: 6,           // F: 規格
  quantity: 7,       // G: 数量
  unitPrice: 8,      // H: 単価
  subtotal: 9,       // I: 小計（税抜）
  tax: 10,           // J: 消費税
  total: 11,         // K: 合計（税込）
  deliveryDate: 12,  // L: 納品日
  note: 13,          // M: 備考
};

// ───────────────────────────────────────
// ステータス定義（案件の進行状態）
// ───────────────────────────────────────
const STATUS = {
  draft: '見積作成',      // 初期状態
  ordered: '注文確定',    // 受注した
  delivered: '納品済み',  // 納品が完了した
  invoiced: '請求済み',   // 請求書を送付した
  paid: '入金済み',       // 入金を確認した
};

// ───────────────────────────────────────
// 税率（消費税は10%固定。軽減税率対応が必要な場合はここを修正）
// ───────────────────────────────────────
const TAX_RATE = 0.10;

// ───────────────────────────────────────
// 帳票の共通設定
// ───────────────────────────────────────
const DOC_SETTINGS = {
  paperSize: 'A4',                    // 用紙サイズ
  dateFormat: 'yyyy年MM月dd日',       // 日付フォーマット
  currencyFormat: '#,##0',            // 金額フォーマット（3桁カンマ区切り）
  estimateValidDays: 30,              // 見積書の有効期限（日数）
  paymentTerms: '月末締め翌月末払い',  // デフォルトの支払条件
};
